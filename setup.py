from setuptools import setup

setup(
   name='pysvg',
   version='1.0',
   description='Simple diagram tool for SVG',
   author='Djordje Obradovic',
   author_email='obradovic.djordje@gmail.com',
   packages=['pysvg'],  #same as name
   install_requires=[], #external packages as dependencies
)
